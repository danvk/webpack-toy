var webpack = require('webpack');

var config = Object.create(require('./webpack.config.prod.js'));
config.plugins = [new webpack.optimize.UglifyJsPlugin()].concat(config.plugins);
config.output.filename = '[name].min.js';

module.exports = config;
